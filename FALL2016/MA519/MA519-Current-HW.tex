\def\hwnum{3}
\def\due{September 15, 2016}
\def\auth{Carlos Salinas}
\def\tight{MA 519: Homework \hwnum}
\def\short{MA519-HW-\hwnum}
\def\class{MA 51900}
\def\subject{elementary probability}
\def\email{salinac@purdue.edu}

\documentclass[article,oneside]{memoir}
\usepackage{geometry}

%% Base customization and variables
\usepackage{clos-vars}

%% Formatting

%% Footnote style
\renewcommand*{\thefootnote}{\fnsymbol{footnote}}

%% Hyperref setup
\hypersetup{%
  breaklinks,%
  colorlinks=true,%
  linkcolor=black,%
  citecolor=black,%
  filecolor=black,%
  menucolor=black,%
  runcolor=black,%
  urlcolor=black,%
  pdftitle={\short},%
  pdfauthor={\auth},%
  pdfkeywords={\subject},%
  pdfsubject={\class},%
  pageanchor={false},%
  unicode
}

%% Graphics path as it says
\graphicspath{{figures/}}

\newcounter{problem}
\renewenvironment{problem}[1][]% environment name
{% begin code
  \stepcounter{problem}
  \par\vspace{\baselineskip}\noindent
  \ifx &#1&%
  {\Large\textbf{Problem~\hwnum.\theproblem}}
  \global\def\exname{Problem~\hwnum.\theproblem}%
  \else
  {\Large\textbf{Problem~\hwnum.\theproblem~(#1)}}
  \global\def\exname{Problem~\hwnum.\theproblem~(#1)}
  \fi
  \par\vspace{\baselineskip}%
  \noindent\ignorespaces
  % \begin{linenumbers}
}%
{% end code
  \par\vspace{\baselineskip}%
  \noindent\ignorespacesafterend
  % \end{linenumbers}
}

\numberwithin{equation}{subsection}
\renewcommand\theequation{\hwnum.\arabic{equation}}

\begin{document}
\def\authst{Carlos Salinas,\quad Max Jeter}
\def\auths{Carlos Salinas, Max Jeter}
\frontmatter \aliaspagestyle{title}{empty} \pagestyle{title}
% \authsor{\href{mailto:\email}{\auths}}
\author{\authst}
\title{\tight}%
\date{\due}%
\maketitle
\cleartooddpage
\makepagestyle{my-headings}
\makeoddhead{my-headings}
        {\small{\MakeUppercase{\emph\auths}}}
        {}
        {\small{\MakeUppercase{\emph\exname}}}
\makeoddfoot{my-headings}{{\emph\tight}}
                      {}
                      {\thepage}
\makeevenhead{my-headings}
        {\small{\MakeUppercase{\emph\auths}}}
        {}
        {\small{\MakeUppercase{\emph\exname}}}
\makeevenfoot{my-headings}{{\emph\tight}}
                      {}
                      {\thepage}
\makeheadrule{my-headings}{\textwidth}{.25pt}
\pagestyle{my-headings}
% \makerunningwidth{headings}{1.15\textwidth}
\mainmatter
\include{homework/ma519-\hwnum}
\end{document}

%%% Local Variables:
%%% mode: latex
%%% TeX-master: t
%%% End:
